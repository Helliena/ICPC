#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<ll, ll> pll;

void acceleration(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}

void solve();


int main() {
    //acceleration();
    solve();
}

void solve() {
    double n, m, w, v0, k, c, p, t, r;
    cin >> n >> m >> w >> v0 >> k >> c >> p >> t >> r;
    cout << fixed;
    cout << setprecision(30) << k << endl;
}
